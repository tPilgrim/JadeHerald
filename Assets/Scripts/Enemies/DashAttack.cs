using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DashAttack : MonoBehaviour
{
    /*
    public GameObject EnemyComponent;
    private GameObject Player;
    public int Damage;
    private bool HitShield;

    void Start()
    {
        Player = GameObject.FindGameObjectWithTag("Player");
    }

    private void OnTriggerEnter2D(Collider2D other)
    {
        if(other.gameObject.tag == "Player")
        {
            /*
            if(EnemyComponent.GetComponent<Snarkh>() != null)
            {
                EnemyComponent.GetComponent<Snarkh>().DashDamage(Damage, HitShield);
            }

            if(EnemyComponent.GetComponent<Scout>() != null)
            {
                EnemyComponent.GetComponent<Scout>().DashDamage(Damage, HitShield);
            }
            
        }
    }

    private void OnTriggerStay2D(Collider2D other)
    {
        if(other.gameObject.tag == "Sheild")
        {
            HitShield = true;
        }
    }

    private void OnTriggerExit2D(Collider2D other)
    {
        if(other.gameObject.tag == "Sheild")
        {
            HitShield = false;
        }
    }
*/
}
